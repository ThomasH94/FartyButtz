using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This class will control which game mode will be selected and tell the scene controller to load the appropriate stage
/// based on the game mode passed in
/// This class will be used in conjunction with the Game Manager to decide which stage, which mode, which Butt, etc
/// </summary>
public class GameModeManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
